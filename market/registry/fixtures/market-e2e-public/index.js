// Acceptance fixture only. This package is readable by anonymous clients, so
// reaching it proves the market proxied it without a service account.
export const fixture = '@tokensapi/market-e2e-public'
