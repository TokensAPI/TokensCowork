{
  "name": "tokenscowork-market",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node ops/market-check.mjs",
    "build": "node ../scripts/generate-market-catalog.mjs",
    "verify": "npm run check && npm test && node ../build/verify/market-catalog-verify.mjs"
  }
}
