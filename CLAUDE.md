# 提交规范 / Commit conventions

- 使用 Conventional Commits（例如 `fix(desktop): ...`、`docs: ...`）。
- **禁止**在提交消息中追加 `Co-Authored-By: ...` 等 AI 工具署名尾部：这类尾部会被 GitHub 计为独立贡献者，历史上已因此把 AI 显示进 Contributors 并全部清除，务必不要再加。
- Adhere to Conventional Commits. Never append a `Co-Authored-By:` (or any AI-tooling) trailer to commit messages.
